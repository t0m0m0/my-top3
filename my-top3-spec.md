# My Top 3 - サービス仕様書 v1.1

## 概要
テーマに沿った書籍、音楽、映画の3作品をキュレーションし、画像を生成してSNSにシェアできるWebサービス

**コンセプト**: 「雨の日に楽しむ」「失恋したときに」など、テーマ性のある作品キュレーションを通じて、自分の趣味や感性を表現・共有する

---

## 画面構成

### メイン検索ページ（トップページ）

#### レイアウト構成
```
┌─────────────────────────────────────┐
│   テーマ入力エリア                   │
├─────────────────────────────────────┤
│   選択状態エリア（上部固定）          │
│  [Book] [Music] [Movie] + 作成ボタン  │
├─────────────────────────────────────┤
│  タブ: [Book] [Music] [Movie]       │
├─────────────────────────────────────┤
│  検索バー                            │
├─────────────────────────────────────┤
│  検索結果 / 検索履歴                 │
│  （無限スクロール）                   │
└─────────────────────────────────────┘
```

#### テーマ入力エリア
- **入力方法**: フリーテキスト入力
- **入力欄**: テキストボックス（最大50文字程度）
- **プレースホルダー**: 「例: 雨の日に楽しむ3作品」
- **説明テキスト**: 「テーマを入力してください（例: 失恋したときに、元気を出したいときに）」
- **必須/任意**: 任意（テーマなしでも作成可能）
- **配置**: 選択状態エリアの上または下

#### 選択状態エリア（上部固定）
- 3枠固定表示（Book / Music / Movie）
- **未選択時**: 空欄表示（"未選択" または "Book #1" 等のプレースホルダー）
- **選択済み時**: 小さいカード形式でサムネイル + タイトル表示
- **選択解除**: 選択済みアイテムをタップで解除
- **作成ボタン**: 3つすべて選択完了で「Top3を作成」ボタン出現
  - ボタン押下でTop3表示ページへ遷移

#### タブ切り替え
- Book / Music / Movie の3タブ
- タブ切り替えで以下がすべて切り替わる:
  - 検索バー（独立）
  - 検索履歴
  - 検索結果

#### 検索機能
- **検索方式**: リアルタイム検索（入力中に候補表示）
- **検索対象**: タイトル、著者名/アーティスト名/監督名すべて
- **結果表示**: 無限スクロール（ページネーションなし）
- **初期表示**:
  - 検索履歴がある場合: タブごとの検索履歴を表示（最新5-10件）
  - 検索履歴がない場合: 空状態メッセージ（「作品名を検索してください」等）

#### 検索履歴
- **保存先**: ブラウザのローカルストレージ
- **保存単位**: タブごと（Book/Music/Movie別々）
- **保存内容**: 検索キーワード + タイムスタンプ
- **表示形式**: 「最近検索したキーワード」ラベル付き
- **クリック動作**: 履歴タップで再検索実行
- **削除機能**:
  - 個別削除: 各履歴項目に×ボタン
  - 全削除: 「履歴をすべて削除」ボタン

#### 検索結果カード
各カードの表示項目:
- サムネイル画像
- タイトル
- サブ情報（著者名 / アーティスト名 / 監督名）
- 「#1に選ぶ」ボタン

**選択状態管理**: 
- 各カテゴリで1作品のみ選択可能
- 選択時は上部の選択状態エリアに反映
- 既に選択済みの場合は置き換え

---

### Top3表示ページ

#### URL構造
```
/my-top3?theme={theme_text}&book={book_id}&music={music_id}&movie={movie_id}
```
- `theme`: テーマテキスト（URLエンコード、任意パラメータ）
- `book`, `music`, `movie`: 各APIの作品ID
- URLパラメータから作品情報とテーマを再取得して表示

#### 表示内容
- **画像サイズ**: 1080x1080px（Instagram正方形フォーマット）
- **レイアウト**: 
  - 上部: テーマテキスト（入力されている場合のみ表示）
  - 中央: 横並び3分割
    - 左: 書籍
    - 中央: 音楽
    - 右: 映画

**画像構成例**:
```
┌─────────────────────────────────────┐
│   「雨の日に楽しむ3作品」           │
├─────────────────────────────────────┤
│        作品画像（3分割）             │
│   [Book] [Music] [Movie]            │
└─────────────────────────────────────┘
```

各作品の表示項目:
- サムネイル画像
- カテゴリバッジ（BOOK / MUSIC / MOVIE）
- #1バッジ
- タイトル
- サブ情報（著者名 / アーティスト名 / 監督名）

#### 機能
- **画像保存**: 生成された画像をダウンロード
- **URLコピー**: 現在のURLをクリップボードにコピー
- **SNSシェア**: Twitter/X等へシェア
- **外部リンク**: 各作品の詳細ページへのリンク
  - 書籍: Amazon商品ページ（ISBN経由、アフィリエイトリンク対応）
  - 音楽: Spotify作品ページ（APIから取得した直リンク）
  - 映画: IMDb作品ページ（`https://www.imdb.com/title/{imdb_id}`形式）

---

## データ保持方式

### URL埋め込み（メイン）
- クエリパラメータで作品IDを保持
- 認証・データベース不要
- URLシェアで他ユーザーも同じTop3を閲覧可能

### ローカルストレージ（サブ）
- **検索履歴**: タブごとに保存（キーワード + タイムスタンプ）
- **テーマ履歴**: 最近使用したテーマ（5-10件）
- **前回選択（オプション）**: 直前に選んだTop3を記憶

---

## 外部API仕様

### Google Books API
- **用途**: 書籍検索・情報取得
- **取得データ**: 
  - 作品ID
  - タイトル
  - 著者名
  - サムネイル画像URL
- **必要な認証**: APIキー

### Spotify Web API
- **用途**: 音楽（アルバム・楽曲）検索・情報取得
- **取得データ**:
  - 作品ID
  - タイトル（アルバム名 / 楽曲名）
  - アーティスト名
  - アルバムアートワークURL
- **必要な認証**: Client Credentials Flow

### TMDb (The Movie Database) API
- **用途**: 映画検索・情報取得
- **言語設定**: `language=ja-JP`パラメータで日本語対応
- **取得データ**:
  - 作品ID（TMDb ID）
  - IMDb ID
  - タイトル（日本語）
  - 監督名
  - ポスター画像URL
- **必要な認証**: APIキー

---

## 技術要件

### フロントエンド
- React
- Tailwind CSS
- React Router（ページ遷移）
- 画像生成ライブラリ（HTML to Canvas変換）

### API通信
- REST API呼び出し
- エラーハンドリング
- レート制限対応

### デプロイ先（推奨）
- Azure Static Web Apps
- または Vercel / Netlify

---

## プロジェクト構成

### 推奨ディレクトリ構成
```
my-top3/
├── src/
│   ├── components/      # UIコンポーネント
│   ├── pages/          # ページコンポーネント
│   ├── hooks/          # カスタムフック
│   ├── services/       # API通信ロジック
│   ├── utils/          # ユーティリティ関数
│   ├── App.jsx
│   └── main.jsx
├── public/
├── .env.example
├── package.json
└── README.md
```

### 主要コンポーネント
- SearchBar: 検索入力
- TabSwitcher: タブ切り替え
- SearchHistory: 検索履歴表示
- SearchResults: 検索結果一覧
- ResultCard: 検索結果カード
- SelectionArea: 選択状態エリア
- Top3Image: Top3画像生成・表示

---

## 実装の優先順位（MVP）

### Phase 1: 基本機能
1. プロジェクトセットアップ
2. 外部API統合
3. 検索機能（リアルタイム検索）
4. タブ切り替え
5. 選択状態管理
6. 検索結果表示（無限スクロール）
7. **テーマ入力UI**
8. **テーマのURL埋め込み**

### Phase 2: 選択・表示機能
9. 選択状態エリアUI
10. Top3表示ページ（**テーマ含む**画像生成）
11. URL埋め込み機能

### Phase 3: UX向上
12. 検索履歴機能
13. **テーマ履歴機能**
14. 画像保存機能
15. シェアボタン
16. レスポンシブ対応

### Phase 4: 仕上げ
17. エラーハンドリング
18. ローディング状態表示
19. アニメーション・トランジション

---

## エラーハンドリング仕様

### API関連エラー
- **画像取得失敗**: デフォルト画像（No Image）を表示
- **API制限**: 「しばらく時間をおいて再度お試しください」メッセージ
- **検索結果0件**: 「結果が見つかりませんでした」メッセージ

### URL関連エラー
- **存在しないID**: エラーメッセージ + トップページへのリンク
- **パラメータ不足**: 「Top3を作成してください」メッセージ

### 一般的なエラー
- ユーザーフレンドリーなエラーメッセージを表示
- 必要に応じて再試行オプションを提供

---

## レスポンシブ対応

### ブレークポイント
- **モバイル**: < 640px
- **タブレット**: 640px - 1024px
- **デスクトップ**: > 1024px

### モバイル対応
- 検索結果カード: 1カラム表示
- 選択状態エリア: 横スクロールまたは縦積み
- Top3画像: 縦並び3分割に変更

### タブレット対応
- 検索結果カード: 2カラム表示
- Top3画像: 横並び維持（スケール調整）

---

## 制約事項（MVP範囲外）

### 含まない機能
- コメント機能
- ユーザー認証
- データベース
- 過去のTop3履歴（URL保存以外）
- カスタマイズ機能（背景色・レイアウト変更等）
- OGP対応（SNSプレビュー画像）

### API制限
- 各APIの無料枠内での利用
- レート制限に注意

---

## 将来的な拡張候補（Phase 2以降）

### サービスの差別化
**Topsters（競合）との違い**:
- Topsters: 大量（40-100個）のランキング、永遠の殿堂入り
- このサービス: 3作品のみ、**テーマ性のあるキュレーション**（シチュエーション別おすすめ）

### 機能拡張
- データベース導入（Firebase / Supabase）
- ユーザー認証（Google / Twitter OAuth）
- コメント機能（各作品への一言）
- 過去のTop3履歴・ギャラリー
- みんなのTop3フィード
- カスタマイズ機能（テーマ・レイアウト選択）
- OGP対応（サーバーサイドレンダリング）
- PWA化（オフライン対応）

### 収益化機能
- **広告配置**（控えめパターン推奨）
  - 検索結果下部にバナー広告（1箇所）
  - Top3画像生成後に小さめバナー
  - Google AdSense導入
- **アフィリエイト**
  - Amazonアソシエイト（書籍リンク）
  - 既に実装済みのリンク機能を収益化に活用

---

## 環境変数

以下の環境変数が必要:

```
VITE_GOOGLE_BOOKS_API_KEY=
VITE_SPOTIFY_CLIENT_ID=
VITE_SPOTIFY_CLIENT_SECRET=
VITE_TMDB_API_KEY=
```

---

## 完成イメージ

### メイン検索ページ
- クリーンでシンプルなUI
- 直感的なタブ切り替え
- スムーズなリアルタイム検索
- 選択状態が一目でわかる上部エリア

### Top3表示ページ
- インパクトのある画像
- SNSシェアしやすいURL
- ワンクリックで画像保存

---

## 補足事項

### データフロー
1. **テーマ入力**（任意）
2. ユーザーが検索バーに入力
3. リアルタイムで外部APIを呼び出し
4. 検索結果を無限スクロールで表示
5. ユーザーが各カテゴリから1作品ずつ選択
6. 3作品選択完了で「Top3を作成」ボタン表示
7. ボタン押下でURLパラメータ（**テーマ含む**）付きのTop3ページへ遷移
8. URLパラメータから**テーマと**作品情報を再取得
9. **テーマ付き**画像生成・表示・シェア機能提供

### 画面遷移
```
トップページ（検索・選択）
  ↓
  3作品選択完了
  ↓
Top3表示ページ（画像生成・シェア）
```

---

**仕様書バージョン**: v1.1  
**最終更新日**: 2025年2月7日  
**主な変更点**: テーマ機能の追加、サービスコンセプトの明確化
